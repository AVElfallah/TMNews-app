import 'package:flutter/material.dart';

class CurrencyNewsPage extends StatelessWidget {
  const CurrencyNewsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
